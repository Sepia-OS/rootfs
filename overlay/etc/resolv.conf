# Rewritten whenever the network comes up: by udhcpc's lease script when an
# interface is on DHCP, and by sepia-network when it is on a static address.
# Anything added here by hand is lost at that point.
#
# Empty on a system that has not been on a network yet - which is not a fault,
# and is what `sepia-network status` reports as having no nameservers.
